cat > README.md << 'EOF'
# WireGuard + wg-easy Auto Setup

Автоматическая установка WireGuard VPN с веб-интерфейсом wg-easy.

## Использование
```bash
bash wg-easy-setup.sh
```

## Требования
- Ubuntu 22.04 / Debian 12
- Root доступ
EOF

git add README.md
git commit -m "Add README"
git push
